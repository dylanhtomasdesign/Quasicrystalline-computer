# =============================================================================
# Senemosìa - Core Dependencies Only
# =============================================================================
# Lightweight requirements for development container
# Heavy packages (torch, tensorflow) are mounted via volume or installed separately
#

# Core scientific computing
numpy>=1.24.0,<2.0
scipy>=1.10.0,<2.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter environment (minimal)
jupyterlab>=3.6.0
ipywidgets>=8.0.0
ipympl>=0.9.0

# Code quality
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Utilities
tqdm>=4.65.0
h5py>=3.9.0
pillow>=10.0.0
